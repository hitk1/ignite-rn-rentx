import React from 'react';
import Backbutton from '../../components/Backbutton';

import {
    Container,
    Header
} from './styles';

export const CarDetails = () => {
    return (
        <Container>
            <Header>
                <Backbutton
                    onPress={() => { }}
                />
            </Header>
        </Container>
    );
}